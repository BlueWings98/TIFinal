apiVersion: v1 
kind: Service
metadata:
  name: balancer
  labels: 
    pp: my-nginx
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
  selector: 
    app: nginx
  type: LoadBalancer